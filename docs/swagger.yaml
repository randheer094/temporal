info:
  contact: {}
paths:
  /events:
    post:
      consumes:
      - application/json
      description: Accepts arbitrary JSON (object or array); rules.yaml extracts log
        entries.
      operationId: log-event
      parameters:
      - description: Arbitrary JSON payload
        in: body
        name: body
        required: true
        schema:
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Log an event
swagger: "2.0"
